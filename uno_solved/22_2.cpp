//I'm writing code.
/*#include<iostream>
#include<algorithm>
#include<vector>

using namespace std;

void aux_ftn(vector<string>& vec, int n, int flag)
{
	if (flag > n)
		return;
	if (flag < 0)
		return;

	if (flag < n)

}
vector<string> generateParenthesis(int n)
{
	vector<string> myvec;
	myvec.push_back("");
	aux_ftn(myvec, n, 0);
	return myvec;
}

int main()
{
	vector<string> myvec = generateParenthesis(10);
	for (int i = 0; i < myvec.size(); i++)
	{
		cout << myvec[i] << endl;
	}
}
*/